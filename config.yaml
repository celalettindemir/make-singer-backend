server:
  port: "3000"
  env: "development"

redis:
  addr: "localhost:6379"
  password: ""
  db: 0

jwt:
  secret: "your-super-secret-key-change-in-production"
  expiration: 24 # hours

ratelimit:
  lyrics_per_min: 30
  render_per_hour: 5
  master_per_hour: 10
  export_per_hour: 20
  upload_per_hour: 50
